# 發布驗證 — v0.30

本節是目前檢查清單；文末保留 v0.23／v0.2／v0.1 的歷史結果，舊工具地圖、版本號、題目與「沒有 lint」敘述只適用當時。`docs/v0.24-validation.md` 至 `v0.29-validation.md`、migration 與 audit 同樣是歷史紀錄，不應拿來覆蓋本節。

## 正常工作流程

```sh
npm ci
npm test
npm run lint
npm run typecheck
npm run build
npm run preview
```

腳本以 `package.json` 為準，沒有 `test:e2e`。CI 執行 npm ci、npm test、npm run build；本機發布前也要跑 lint、typecheck。`npm run dev` 用於開發，正式驗證以 dist 的 preview 為準。禁止把 preview 成功等同已部署。

## 內容

- 所有預期的繁中／英文都有內容；英文不得殘留中文名稱或 placeholder。頁尾版本來自 package.json，0.30.0 顯示 v0.30。
- 六工具名稱一致；電子顯微鏡的知識卡、筆記本及回顧分列 SEM（掃描式電子顯微鏡）表面、TEM（穿透式電子顯微鏡）薄樣品內部。
- SEM 的立體感不等於 3D 模型；TEM 不會透視任意厚樣品。倍率不等於解析度。
- 四題真實影像初始題幹／observation／caption／alt 不洩漏拍攝機制；依觀察問題能選工具，不需先開提示。不能只憑彩色認定螢光。
- 課程真實影像直接顯示；偵探觀察先於圖說，洋蔥站完整介紹不同樣品，其他用短 badge。螢光照片與示意的紅色意義不同。

## 每張外部真實影像

逐一核對 `src/data/images.ts` 八筆 real（課程四張、任務四張），不要只看一張：

- public 檔案存在且瀏覽器成功解碼；自然尺寸與 metadata 一致，完整 contain、原比例尺未裁切。
- sourceUrl 是有效且已記錄的來源說明頁；creator／source、license、適用的 licenseUrl、originalTitle（已有時）均保留。
- 短版作者／授權常駐，details 可用 Enter／觸控展開來源、變更與標記紀錄。CC BY 保留作者、授權連結、來源與修改狀態。
- changes 記錄實際縮小／WebP 轉換；沒有裁切就不聲稱裁切。源檔位於 source_images，production 不讀取該目錄。
- Public Domain、CC0、CC BY 分別依原記錄顯示；不把只適用美國的公有領域聲明擴張成全球。疑義需記錄並請人確認。
- 全站 Credits 與 notebook 的來源資料和照片一致，所有外部連結使用安全的新分頁屬性。

## Responsive 與 accessibility

至少測 360×800、390×844、430×932、768×1024、1024×768、1440×1000，兩種語言均測：

- 無水平捲動；照片有高度上限、完整視野、字級可讀，繁中與長英文名稱自然換行。
- 課程照片後「往下繼續」可到主要操作，任務選項不被來源細節推得太遠；檢查實際捲動與 fold 提示。
- 按鈕和來源 summary 至少 44px 高；不用 hover 解鎖資訊。短版來源連結與長作者可換行。
- 鍵盤可完成操作、提示、來源展開、知識卡、dialog；焦點可見，Escape 關閉筆記本回到開啟按鈕。下一題／完成頁焦點轉移正常。
- 每張照片 meaningful alt；來源細節隱藏時仍可透過原生 disclosure 存取。檢查文字／背景對比，不靠顏色作唯一線索。
- prefers-reduced-motion: reduce 時互動仍可完成；無必要動畫或平滑捲動。

## 互動與共用電腦

- 從乾淨首頁各走完中英文六站課程、12 題任務；課程不新增必答門檻。筆記本保留六工具卡及任務證據。
- 神秘影像先不開提示作答，再測提示、重載、第一次重試、第二次較強提示、協助及正解。下一題清掉前題提示。
- 切換語言保留當前題目與提示；重載保留有效進度。content 4 的 v0.29 存檔仍可用，較舊內容依原機制失效。
- 「再玩一次」保留課程／語言，「回首頁」保留任務。「下一位小偵探」從完成頁直接清空全部遊戲狀態，回繁中首頁；重載後仍乾淨。
- Next Detective 不清除其他 localStorage 鍵。另測過期、損毀、不相容與 blocked storage，記憶體內仍可遊玩。

## v0.30 實際驗證紀錄（2026-09-22）

- `git fetch origin` 成功；local／origin dev/v0.29 均為 `eeb2bf7fc9bb9b8e1e407a4261690b819403a8c1`。原 dev/v0.30 已存在於包含此提交的 `a893fe1`，初始 tree 與 v0.29 相同；沿用原分支，未重寫歷史。
- `npm ci --cache .npm-cache --no-audit --no-fund` 成功，161 個套件。首次 sandbox 安裝出現 npm “Exit handler never called”，取得允許的執行權限後相同命令成功。
- `npm test`：42 passed，0 failed；包含新增加的初始任務不洩露機制、可選提示不改答案、compact attribution 常駐作者／授權，以及英文字串無中文檢查。
- `npm run lint`、`npm run typecheck`、`npm run build`、`git diff --check` 通過；`npm run preview` 預設 4173 也成功啟動。建置未修改圖片或來源 metadata。
- `node artifacts/check-v030-metadata.cjs` 對照 v0.29，八筆 credit 物件逐欄相同，圖片路徑／尺寸及 source_images／WebP／SVG 完全保留，dist 圖片位元與 public 相同。
- 以 `node node_modules/vite/bin/vite.js preview --host 127.0.0.1 --port 5178 --strictPort` 啟動 production preview。PowerShell 的 npm wrapper 未正確轉送首次指定 port 參數，改用等效 Vite CLI；不是 dist 建置失敗。
- 沿用既有本機 Playwright／headless Microsoft Edge（不增加依賴或測試框架）。`npx tsx artifacts/v030-fixtures.ts` 產生目前資料，`node artifacts/check-v030.cjs` 通過上述六種 viewport × 兩語 × 八張照片，共 96 組；無水平溢出或 pageerror。
- `node artifacts/check-v030-completion.cjs` 通過 1440px／360px 完成頁換行與 notebook Escape 焦點返回；人工檢視桌機完成頁。
- Browser 實走完整六站及十二題，各中英文一次；通過 hints／strong hints、來源鍵盤展開、筆記本 SEM/TEM、Next Detective／重載清空／不影響其他鍵、語言與提示保存、reduced-motion。
- 人工檢視 360px 英文螢光、360px 中文 SEM 任務、平板中文 TEM、桌面英文血球截圖：完整視野、可讀換行及來源層級正常。保留的 artifacts/v030-browser-results.json 與 v030-*.png 為本機驗證產物，依既有規則不納 Git；檢查清單可手動重現，不宣稱有配置 CI browser suite。
- 八張 Commons 原始來源頁均能開啟，逐張核對現有授權；保留 NICHD 的 CC BY 2.0，未因機構名稱改稱 Public Domain。未下載或替換圖片。

以上是桌面瀏覽器的 viewport 測試，不等於實機 iOS／Safari、螢幕閱讀器或 WCAG 認證。仍需兒童與家長試玩、手機觸控／捲動、科學措辭的人員審閱。未發現現行八筆授權與來源頁矛盾；歷史候選清單的 modification 只是範例，未啟用素材須另行核對。沒有合併 main、推送或部署。

---

# 歷史驗證紀錄（非目前發布清單）

# v0.23 驗證紀錄 — 2026-09-16

## 自動檢查

- `npm run build`：TypeScript 與 Vite production build 通過，套件 0.2.3／顯示 v0.23。
- `npm test`：25 項通過。保留任務正解、提示、協助、完整徽章流程、進度保存與重設測試；課程資料檢查改為六站無正解／選項，新增兩個螢光圖層的對齊與獨立訊號驗證。
- `git diff --check`：通過。專案沒有 lint script。

## 瀏覽器實測

使用本機 production preview，以桌面 1280×900 與手機直向 390×844 viewport 檢查，非實體手機測試。

- 首頁顯示 v0.23 與「🔬 偵探訓練所」，進入後先見簡短工具地圖。
- 六站依序完成操作、發現、收藏，地圖顯示 6/6。課程沒有 A/B/C、正誤或答題通關。
- 肉眼切換遠近；放大鏡方向按鈕掃到翅膀後觸發線索。手機畫面中的鏡片與方向按鈕可見且無水平溢出。
- 解剖倍率可切至高倍率；視野 slider 的方向鍵會更新位置。
- 光學初始模糊；以 Home／PageUp 鍵調到 60（示意最佳焦點 65 ±8）後顯示清晰發現，可切 40×比較視野。
- 螢光驗證細胞核單獨顯示、兩者疊合、關閉細胞核只留粒線體。手機圖層與按鈕排版正常。
- 電子可切 TEM 內部與 SEM 表面，沒有錯誤選項；兩者探索後顯示依需求選工具的結論。
- 重新載入保留收藏；完成訓練後進入原任務，第一題仍可複選、判答成功並累積證據（6 張課程卡 + 1 份任務證據）。
- 檢查期間瀏覽器 error log 為空。桌面圖片最大寬度調整為 600px，避免過大影像擠遠控制區。

## 素材與範圍限制

- 全部影像是有標示的原創教學示意，並非真實顯微照片。沒有新增假照片或假蛋白質 channel。
- 螢光使用現有細胞核／粒線體兩種圖形拆層；尚無特定蛋白質或細胞骨架的實拍 channel。
- 倍率、焦距與果蠅遠近是概念操作，未校準實際尺寸／物鏡倍率；平面畫面不模擬真正的雙眼立體感。
- 沿用現有 state、storage key、schema、CONTENT_VERSION 與 academyRevision，v0.22 有效存檔可續用。探索中的焦點／倍率／channel 不另存，重開該站重置，但收藏保留。
- 未變更 deployment、GitHub Pages、CNAME、Vite 設定或依賴；未部署。

---

以下保留先前版本紀錄。

# v0.22 驗證紀錄 — 2026-09-16

## 自動化與建置

- `npm test`：24 / 24 通過。包含四題各選項判答、果蠅重試／協助正解、舊 content 2 完成紀錄拒收、現版存檔續玩、命名一致性與科學內容檢查；既有任務全組合與完整流程測試保留。
- `npm run build`：TypeScript 與 Vite production build 通過，套件 0.2.2／顯示 v0.22。
- repository search：舊完整果蠅題幹與尺度任務「看清楚整體外形」已移除；未加「複式」的光學顯微鏡名稱僅保留在大類關係說明。歷史 changelog／驗證紀錄保留原版描述。
- `git diff --check` 通過。未變更 components、reducer、routing、CSS、依賴或部署設定。

## 正式預覽實測

- 使用本機 production preview，首頁版本顯示 v0.22；課程與工具選項顯示「複式光學顯微鏡」。
- 完整果蠅題：選肉眼顯示可見但不適合仔細觀察的提示；第二次選放大鏡顯示可用但非最佳工具；選解剖顯微鏡通過。
- 葉脈題使用現有葉片素材，放大鏡正解通過；中英切換保留作答。
- 390 × 844 手機直式：長工具名稱與回饋無水平溢出（document scrollWidth = clientWidth = 375，另有垂直捲軸）；桌面預設視窗也可完成上述操作。
- 本次瀏覽器未記錄 warning／error。沒有進行實體手機觸控、完整無障礙稽核或線上部署驗收。

## 進度相容性

正解改動依內容編輯規則提升 `CONTENT_VERSION` 至 3。舊 v0.2／v0.21 任務、課程收藏與練習進度依既有驗證機制重置；原 v0.1 儲存鍵不動。沒有新增遷移演算法或改動判答機制。

---

# v0.21 驗證紀錄 — 2026-09-15

依 PROJECT_BRIEF.md，沿用 React、reducer、原題庫及卡片樣式，沒有新增應用依賴。

## 自動化與正式輸出

- `npm test`：19 / 19 通過。既有十二題任務、重試與協助、語言、存檔／過期／重設；新增替代答案、練習與任務隔離、舊四課索引遷移、重新讀取及鏡片邊界／放大中心檢查。
- `npm run build`：TypeScript 與 Vite 通過，輸出 `dist/`。
- 正式預覽根路徑：首頁與 13 個資源（JS、CSS、favicon、10 張教學 SVG）全部 HTTP 200。
- CNAME、.nojekyll 與所有圖檔已出現在 dist；入口資源使用 `./`，沒有假設 `/micro-detective/`。
- Git diff 空白檢查通過。

## 瀏覽器實際操作

- 桌機、390 × 844 手機直式：課程導航、放大鏡、解剖顯微鏡與問答可操作；手機放大鏡頁無水平溢出，四個方向按鈕為 68 × 60 px。
- 放大鏡：實際拖曳到葉脈會顯示發現；方向鍵與方向按鈕也能移動並解鎖檢核。
- 完成新增兩課的操作、檢核和收藏；筆記本顯示六張知識卡及正確收藏狀態。
- 四題小遊戲完整通關：繁中、英文切換；連續兩次答錯得到強提示；放大鏡／解剖顯微鏡替代答案得到各自科學解說。
- 完成技能後重新整理保留完成訊息、語言與收藏；可接到原本 12 題任務，任務中能打開筆記本查看技能。
- 正式靜態建置在瀏覽器正常顯示 v0.21。

## 驗證範圍

本次以桌機瀏覽器及手機尺寸驗證，包含 pointer 拖曳、觸控大小按鈕與 keyboard 操作。未使用實體手機／平板測試手指多點觸控，尚未在正式 GitHub Pages 或網域上執行部署驗收。插圖繼續明確標示為教學示意，非真實顯微影像。

---

# v0.2 驗證紀錄

執行日期：2026-09-15。沿用 v0.1 的實際應用與測試；原版紀錄保留在下方。

## 自動化與建置

`npm test`：14 項通過。涵蓋 16 種複選組合、其餘每題的全部選項、完整 12 題流程、兩次後協助、重複回答不重複獎勵、固定最終三步、雙語／重載／重玩、課程進度與任務切換、非法課程 ID、收藏去重、舊存檔保留、存檔過期／損毀／受阻、雙語內容與 10 張本機 SVG 素材。

`npm run build`：TypeScript 與 Vite 正式建置通過。`git diff --check` 無內容空白錯誤。未新增應用依賴；套件與 lockfile 同步為 0.2.0。

## 瀏覽器實測

- 首頁直接開始任務，不必先完成課程。
- 完整通過 12 份任務證據及最終徽章，確認三步調查順序與科學問題選工具的結論。
- 第一題連續答錯兩次，先得到一般提示，再得到具體強提示與「一起找答案」，仍可正常完成。
- 任務 2 選取後重新整理，選項保留；打開筆記本能借閱四張未收藏卡，Escape 關閉後焦點回開啟按鈕。
- 完成全部四個課程：尺度圖、鍵盤 Enter 開光源、螢光四步、電子內部比較及灰階／後製顏色說明。
- 螢光課程連續選兩個迷思選項，確認第二次提供更強提示；再正確回答並收藏。
- 四張卡收藏完成，課程地圖顯示 4/4 與「看見／尋找／深入」總結；重載後卡片仍在。
- 英文筆記本顯示全部四張知識卡與 12 份證據。
- 重玩對話框取消後仍在原畫面；確認重玩後任務回到 0/12、課程收藏仍為 4、語言仍為英語。
- 「下一位小偵探」重置後回到繁中首頁，預覽留在新玩家入口；瀏覽器未記錄 error／warn。
- 檢查 320、390、768、1024 px 的代表版面：窄版筆記本無橫向溢出；768 px 英文螢光課程正常排版；已檢查畫面的圖片皆載入。
- 保留原本鍵盤焦點、原生 dialog 與 aria-live；新增區域使用原生按鈕。減少動態的 CSS 關閉動畫、轉場與平滑捲動；未改動使用者作業系統設定。

## 科學內容核對

- [Nikon MicroscopyU：螢光原理與標記](https://www.microscopyu.com/techniques/fluorescence/introduction-to-fluorescence-microscopy)
- [Leica：螢光顯微鏡是光學顯微鏡](https://www.leica-microsystems.com/science-lab/microscopy-basics/a-guide-to-fluorescence-microscopy/)
- [NIGMS：電子取代光的觀察方法](https://nigms.nih.gov/biobeat/tag/cryo-electron-microscopy)

依使用者指定的概念，以短句表達光／電子成像、標記訊號、顏色與細節辨識，未加入進階光學理論。沿用的圖片全為明確標示的教學示意，尚未換入真實顯微照片。

## 實際範圍與後續確認

課程每個 1–2 分鐘是設計目標，尚未由 7–12 歲兒童實測。此次測試使用桌面 Chromium 與不同 viewport，非實機 iPad／Safari 或 NVDA／VoiceOver 認證。網站保留原 24 小時本機閒置失效規則；課程未完成的演示會在重載時重開，已收藏卡與任務作答不受模組重開影響。

此次交付為既有專案的本機 v0.2 與預覽；沒有更改線上 Site 的版本或分享權限。

---

# v0.1 驗證紀錄

執行日期：2026-09-14 至 2026-09-15。

## 自動化

`npm test`：10 項測試通過，包含：

- 複選題全部 32 種答案組合。
- 其餘每一題的每個選項及正確證據數。
- 四案件＋五最終影像到完成頁的完整狀態轉移。
- 重複回答不重複得分、未解決不能前進。
- 兩次嘗試後的協助完成與相同徽章資格。
- 雙語切換、螢光步驟、作答與順序保存。
- 每輪 5 張唯一最終影像、四種觀察方式完整涵蓋。
- 重新開始／新玩家的語言與清空規則。
- 存檔過期、損毀、版本不合、受阻以及不清除其他網站資料。
- 所有題目、選项、圖片 ID、雙語內容、10 張原創 SVG 路徑與安全結構。

`npm run build`：TypeScript 檢查與 Vite 正式建置通過。

## 實際瀏覽器試玩

在 Codex 內建 Chromium 瀏覽器，以滑鼠／可及性點選及鍵盤操作本機 Vite 預覽：

- 完整通過四個教學案件、五张最終影像與徽章頁，進度達 9/9。
- 肉眼題先選不合適的細胞，再依提示修正為葉片、成體果蠅、成體斑馬魚。
- 保留選取與提示時切換英語，再重新整理，確認語言與作答恢復。
- 操作螢光四步驟，確認圖像、步驟狀態與說明一致。
- 最終影像以兩次不合適選擇觸發協助，確認仍可收集證據並領徽章。
- 筆記本顯示九份證據；Escape 關閉後焦點返回開啟按鈕。
- 成功作答後聚焦回饋；下一題聚焦新題目。
- 再玩一次歸零；遊戲內取消重新開始保留答案，確認重新開始則清空。
- 檢查繁中／英文以及 320、390、768、1024、1440 px 代表尺寸；檢查手機螢光與平板英文版面、桌面徽章畫面。
- 目前畫面的圖片載入成功，檔案路徑另由完整素材測試檢查。
- 可選的唯讀 WebMCP 工具已驗證註冊、正常讀取進度及拒絕不合法輸入。

## 驗證範圍

以上不代表已完成真實兒童試玩、實機 iPad／Safari、NVDA／VoiceOver 朗讀測試或完整 WCAG 合規稽核。活動前建議由工作人員在實際設備與網路環境跑完一輪，並由設施人員審閱真實圖片替換後的內容。

5–10 分鐘是設計目標，尚待目標年齡孩子的現場試玩確認；遊戲沒有時間限制。
