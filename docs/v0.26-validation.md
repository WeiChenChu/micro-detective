# v0.26 驗證紀錄

日期：2026-09-17。基於 v0.25 的 dev/v0.26；不合併、不推送、不部署。

## 實作範圍

- 沿用 AcademyLesson → KnowledgeCard → DetectiveNotebook，沒有新增工具箱、技能卡、點數或課後測驗。
- AcademyLesson 新增 image、imageAlt、toolName、shortDescription，集中於 src/data/toolIllustrations.ts，保留原有 concept、clue、check 等內容。
- 六張原創 SVG 位於 src/assets/tools/，由 Vite 的 literal new URL 資產管線處理。現有下載 SVG 未納入、未清理。
- 最後一張課程卡收藏完成，以及回到課程首頁時，都提供 AcademyCompletion：優先翻閱筆記本，也可直接出任務。未完成全部課程不顯示此區塊。
- 從回顧入口第一次開啟筆記本，頂部顯示完成提示；六張知識卡維持在證據檔案之前，並可從筆記本開始／繼續任務。
- 正式任務首次未作答的 toolSelection 題目顯示 NotebookHint。提示出現即保存 notebookHintSeen；本次畫面仍可閱讀、關閉或開啟筆記本。
- notebookHintSeen、notebookReviewSeen 為可選布林欄位。保留 schema 2、content 3、academyRevision 2、原儲存鍵與 24 小時效期；有效 v0.25 存檔缺欄位仍可讀。重玩保留提示狀態，下一位小偵探或原有到期重置會重設。

## SVG 自我檢查（A–K）

六張均使用 240 × 210 viewBox、5 單位圓端線條、中性色主體、透明背景。無外部連結、字型、script、raster image 或文字標籤依賴。

| 圖片 | 檢查重點／結果 |
| --- | --- |
| naked-eye.svg | 眼睛外形、虹膜與眉弧；沒有相機外殼或人物表情。 |
| magnifying-glass.svg | 手柄與圓形鏡片、簡單葉脈。縮圖檢查後修正手柄與鏡框的間隙。 |
| stereo-microscope.svg | 雙目、寬基座、完整昆蟲；縮圖檢查後增加鏡頭下方工作距離。 |
| compound-microscope.svg | 目鏡、鏡筒、三個物鏡與轉盤、垂直選用物鏡、載物台／玻片、底部照明示意。 |
| fluorescence-microscope.svg | 同系複式外形、側面光源、藍色激發光路、玻片位置的少量綠／洋紅訊號。 |
| electron-microscope.svg | 直立 column、大型主機／樣品室、獨立控制螢幕；不同於光學工具。 |

已在實際知識卡與六圖並排縮圖中檢查，包含手機圖片高度 132px。工具間區別清楚，無能力等級或戰鬥裝備暗示；SEM／TEM 仍沿用原課程與任務的完整名稱，未拆成主卡。

## 瀏覽器驗證

- 實際由首頁進課程，依序完成肉眼三位置、放大鏡方向按鈕找到翅膀、解剖高／低倍率比較、複式鍵盤對焦與原有概念確認、螢光標記與關燈比較、電子內外比較與原有概念確認。
- 每站收藏後確認工具圖、名稱、指定用途摘要及原科學內容。
- 完成六堂 → 完成區塊 → 翻閱筆記本 → 六張已收藏卡與首次回顧提示；關閉再由相同入口開啟，提示不重複。
- 從筆記本開始正式任務，完成前六個原有題目，到 tools-protein 時看到首次筆記本提示。點入口可回顧卡片，返回仍為原題。
- 重載後提示消失，原題及已收集進度保留；下一工具題 tools-fine 也不再出現。回到課程首頁仍可使用完成區塊。
- 修正新提示與 QuestionCard 的同層 React key 重複問題，改用 notebook-hint- 前綴。修正後重載與後續操作未新增該警告。
- Desktop 1280 × 900、Tablet 768 × 1024、Mobile portrait 390 × 844，另測 320 × 740。六張卡的 scrollWidth 等於 clientWidth；文件寬未超出 viewport。
- 桌面／平板圖片高 154px，手機高 132px，object-fit: contain，具 width/height 與固定 CSS 高度，預留載入空間。六張均 complete 且 naturalWidth > 0，沒有觀察到圖片造成的版面跳動。
- 手機點擊展開原知識線索正常，完成區塊按鈕可換行；窄手機筆記本任務按鈕高 50px。互動不依賴 hover。
- 正式 dist 預覽再次由首頁進課程並開筆記本：6/6 SVG 載入成功，均由 Vite inline 為 SVG data URL，無正式頁面 console error/warning。
- 響應式結果為瀏覽器 viewport 測試，尚未進行實體手機／iPad 或兒童試玩。

## 自動檢查

| 檢查 | 結果 |
| --- | --- |
| npm run lint | 通過 |
| npm run typecheck | 通過 |
| npm test | 33/33 通過；新增舊存檔與提示旗標保存／重玩／下一位玩家測試，以及六圖資產與雙語摘要檢查 |
| npm run build | 通過；TypeScript + Vite production build，73 modules |

Sites build-site 包裝腳本在此 Windows 環境找不到 npm-cli.js，改用同一專案既有 npm run build 成功完成；未改套件依賴或部署設定。

## 下一版可考慮

沒有本次範圍內的已知阻擋問題。可邀請目標年齡兒童做「只看圖辨認工具」試玩，觀察解剖／複式與螢光訊號的辨識效果，再依實際回饋微調。保留既有教材與資訊架構。
