# v0.25-dev 兒童語言精修與驗證

日期：2026-09-17。分支：`dev/v0.25`。套件：`0.25.0-dev`，畫面：`v0.25-dev`。

## 修改範圍

- 資料文案：`src/data/ui.ts`、`academyUI.ts`、`academyData.ts`、`academyExtensions.ts`、`missionData.ts`、`observationData.ts`、`investigationData.ts`、`gameUI.ts`、`gameData.ts`、`images.ts`。
- 元件中的既有字串：`src/components/AcademyHome.tsx`、`ConceptReveal.tsx`、`FluorescenceLab.tsx`、`StereoLab.tsx`。僅改文字，沒有變更事件、狀態、結構或完成條件。
- 版本與文件：`package.json`、`package-lock.json`、`README.md`、`CHANGELOG.md`、本紀錄。
- 沒有修改 CSS、圖片、依賴版本、遊戲狀態、題目 ID、答案或存檔格式。

## 重要文案

- 首頁實際入口使用 `academyUI.intro`，改為「有些東西看得到，卻還看不清楚。一起用不同工具，找找微觀世界的線索！」；共用 slogan／intro 同步修正。
- 尺度任務使用「看出大概的形狀」，成體果蠅仍肉眼可見，仔細看頭、胸、腹與翅膀則選擇有較大視野、立體感的解剖顯微鏡。
- 光學操作先解釋兩個靠近的小細節能否分開看清楚，再命名「解析能力」；保留放大同一張圖片不會增加新細節。
- 螢光改為幫目標做記號、找出位置；圓形細胞核的描述限定在這張示意圖。保留光學家族與非天然色概念。
- 統一 SEM（掃描式電子顯微鏡）與 TEM（穿透式電子顯微鏡），去除中文括號後多餘空格。保留表面形貌、電子掃描表面、電子穿過薄樣品與內部皺摺。
- 最終案件把透光樣品、螢光位置、另備薄切片改成短句；保留不能單靠這張 TEM（穿透式電子顯微鏡）圖辨認蛋白質 X。
- 研究任務不再直接用 explanation 當 strong hint；神秘影像第二層提示補充觀察原理，原因留在解說；工具練習使用各自的延伸知識。

## 刻意保留

「看得到，不一定看得清楚；看得清楚，也不一定看得到你想找的線索。」維持原文。工具仍由問題決定，不以倍率排名。電子顯微鏡的特殊準備與活體限制保留「通常」，SEM／TEM 用途保留「常用／適合」，避免過度絕對。示意圖不是同一真實樣品連續放大的限制亦保留。

Academy 維持四堂操作完成、兩堂概念確認；Final Case 維持 Question → Tool → Evidence → New Question。沒有增加題目。

## 驗證結果

- `npm test`：31 項通過，0 失敗。包括雙語內容、答案、存檔相容性、課程結構與最終案件證據流程。
- `npm run lint`：通過。
- `npm run build`：TypeScript 與 Vite 正式建置通過。
- `git diff --check`：通過；元件 diff 與 TypeScript AST 結構比對確認只有字串變動。`src` 全部 SEM／TEM 出現位置均保留完整中文名稱。
- 使用本機 production preview 檢查桌機 1280×900、手機直向 375×812，另抽查最終案件 430×932。
- 手機逐題完成 12 份任務證據、三段最終案件與徽章；重載後保留進度，中文／英文切換正常。
- 抽查六堂課程文字；操作電子課程的表面／內部切換與概念確認、螢光標記與關光比較，均可顯示原有收藏按鈕。
- 首頁、電子課程長回饋、最終案件展開的樣品準備及英語完成頁截圖未見明顯重疊或裁切。手機課程與抽查任務沒有水平溢出。
- 最終案件 `scrollWidth / clientWidth`：375px viewport 為 360/360、430px 為 415/415、1280px 為 1265/1265；差額是垂直捲軸。
- 瀏覽器檢查未見 console warning／error。沒有因文字長度修改 CSS。

## 人工檢查建議

仍需邀請國小三～六年級兒童實際朗讀、試玩，確認「膜、蛋白質、視野、解析能力」的理解與閱讀節奏。此次為瀏覽器 viewport 檢查，尚未做實體手機觸控或 iOS Safari 測試。沒有部署。
